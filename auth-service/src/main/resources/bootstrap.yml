server:
  port: 9002

spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-server:8101
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8001/eureka/
