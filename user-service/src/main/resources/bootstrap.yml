spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8101
      fail-fast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30
