server:
  port: 9004

spring:
  application:
    name: data-retrieval-service
  cloud:
    config:
      enabled: true
      uri: http://config-server:8101
      fail-fast: true
eureka:
  client:
    serviceUrl:
     defaultZone: http://discovery-server:8001/eureka/
